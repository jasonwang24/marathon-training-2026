generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Progress {
  id        Int      @id @default(1)
  data      String   @db.Text
  updatedAt DateTime @updatedAt
}
